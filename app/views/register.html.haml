= form_tag('/register') do
  %fieldset
    %legend Register
    .field
      = text_field_tag(:name, :placeholder => 'Enter your name')
    .field
      = text_field_tag(:email, :placeholder => 'Enter your e-mail address', :value => @email)
    .field
      = submit_tag('Register')
