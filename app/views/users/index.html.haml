%h1 Users

%table
  %thead
    %tr
      %th Name
      %th Functions
      %th Joined

  %tbody
    - @users.each do |user|
      %tr
        %td
          = gravatar_image_tag(user.email, :gravatar => { :size => 50 })
          = link_to(user.name, "/users/#{user.id}")
        %td= user.functions.count
        %td= "#{time_ago_in_words(user.created_at)} ago"
